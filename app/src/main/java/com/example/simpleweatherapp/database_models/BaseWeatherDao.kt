/*

package com.example.simpleweatherapp.model

import androidx.room.Dao
import androidx.room.Insert
import androidx.room.Query

@Dao
interface BaseWeatherDao {

    @Insert
    fun insert(baseWeather: BaseWeatherModel)

    @Query("SELECT * FROM BaseWeatherModel")
    fun getAll(): List<BaseWeatherModel>
}
*/
